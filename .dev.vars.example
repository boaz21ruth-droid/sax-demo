# Copy to .dev.vars for local testing with `npx wrangler dev`.
# In production set the same names in the Worker: Settings → Variables and Secrets.
RESEND_API_KEY=re_xxxxxxxx
BOOKING_TO=booking@example.com
BOOKING_FROM=Website <website@example.com>
# TURNSTILE_SECRET=
